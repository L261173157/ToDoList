<UserControl x:Class="Component.Views.ChatView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="115" d:DesignWidth="450"
             xmlns:prism="http://prismlibrary.com/"
             prism:ViewModelLocator.AutoWireViewModel="True">
    <Grid>
        <ComboBox HorizontalAlignment="Left" Margin="260,0,0,0" VerticalAlignment="Top" Width="80"
                  ItemsSource="{Binding SystemInputSource}" SelectedItem="{Binding SystemInput}" SelectedIndex="0" />
        <Button Content="问答" HorizontalAlignment="Left" Margin="345,0,0,0" VerticalAlignment="Top" Width="100"
                Height="50" Command="{Binding ChatCmd}" />
        <TextBox x:Name="TbTranslate" HorizontalAlignment="Left"
                 Text="{Binding ChatResult,UpdateSourceTrigger=PropertyChanged}" TextWrapping="Wrap"
                 VerticalAlignment="Top" Width="250" Height="100" FontSize="16">
        </TextBox>
        <Button Content="设置" HorizontalAlignment="Left" Margin="345,50,0,0" VerticalAlignment="Top" Width="100"
                Height="50" Command="{Binding ParameterCmd}" />
    </Grid>
</UserControl>