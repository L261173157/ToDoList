<Window x:Class="ToDoList.Views.NewThingView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ToDoList.Views"
        xmlns:prism="http://prismlibrary.com/"
        prism:ViewModelLocator.AutoWireViewModel="True"
        mc:Ignorable="d"
        Title="NewThingView" Height="100" Width="400">
    <Grid>
        <TextBox x:Name="tb1" HorizontalAlignment="Left" Margin="10,0,0,0" Text="{Binding Thing}" TextWrapping="Wrap" VerticalAlignment="Center" Width="294" Height="65"/>
        <Button Content="保存" HorizontalAlignment="Left" Margin="309,0,0,0" VerticalAlignment="Center" Height="31" Width="54" Command="{Binding SaveCmd}" CommandParameter="{Binding Text, ElementName=tb1}" Click="Button_Click"/>

    </Grid>
</Window>
