<Window x:Class="ToDoList.Views.MainView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ToDoList"

        xmlns:prism="http://prismlibrary.com/"
         xmlns:hc="https://handyorg.github.io/handycontrol"
        prism:ViewModelLocator.AutoWireViewModel="True"
        mc:Ignorable="d"
        Title="ToDoList" Height="750" Width="430" WindowStyle="SingleBorderWindow" ShowInTaskbar="False" ResizeMode="NoResize">

    <Grid>

        <hc:NotifyIcon x:Name="NotifyIcon" Text="ToDoList" Icon="/Resources/Journal.ico" MouseDoubleClick="NotifyIcon_MouseDoubleClick" Click="NotifyIcon_Click" />
        <StackPanel Margin="0,35,85,80">
            <DataGrid x:Name="ExaDataGrid" Height="640" ItemsSource="{Binding Things}" AutoGenerateColumns="False" Width="320" HorizontalAlignment="Left" GridLinesVisibility="Vertical" MaxColumnWidth="261" SelectionMode="Single">
                <DataGrid.Columns>
                    <DataGridTextColumn Binding="{Binding Content}" Header="内容" MinWidth="260" CanUserReorder="False" FontSize="14" CanUserResize="False" />
                    <DataGridCheckBoxColumn Binding="{Binding Done}" Header="完成" />
                </DataGrid.Columns>
            </DataGrid>
        </StackPanel>
        <StackPanel Margin="350,35
,0,0">
            <Button Content="新建" Command="{Binding NewThingViewCmd}" Height="40" />
            <Button Content="保存" Command="{Binding RefreshCmd}" Height="40" />
            <Button Content="test" Command="{Binding TestCmd}" />
            <TextBox TextWrapping="Wrap" Text="{Binding Weather}" Width="77" Height="107" />
            <!--<Button Content="Button" Click="Button_Click" />-->
        </StackPanel>
        <hc:SimplePanel Margin="0,675,0,0">
            <TextBox x:Name="tbTranslate" TextWrapping="Wrap" Text="{Binding Translate}" Margin="10,0,205,0" GotFocus="tbTranslate_GotFocus" />
            <Button Content="翻译" Command="{Binding TranslateCmd}" HorizontalAlignment="Left" Margin="341,0,0,0" Height="55" Width="89" />
            <hc:ComboBox x:Name="comTranslateTarget" HorizontalAlignment="Left" Height="55" VerticalAlignment="Center" Width="106" Margin="230,0,0,0" ItemsSource="{Binding TargetItemsSource}" SelectedItem="{Binding Target}" IsReadOnly="True" SelectedIndex="0"/>
        </hc:SimplePanel>
    </Grid>
</Window>